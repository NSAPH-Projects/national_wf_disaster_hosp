# Divide wf disaster data into monthly files to create monthly exposure
# This will be based on ignition dates only
# Libraries ---------------------------------------------------------------
pacman::p_load(tidyverse, here, sfarrow, sf)
# Read --------------------------------------------------------------------
wf_dat <- st_read(here('local_data', 'wfbz_disasters.geojson'))
# Make monthly files ------------------------------------------------------
wf_dat <-
wf_dat %>%
mutate(month_exposed = lubridate::floor_date(wildfire_ignition_date, unit = 'month')) %>%
filter(month_exposed >= as.Date("2000-01-01") & month_exposed < as.Date("2019-01-01"))
# Assign buffers ----------------------------------------------------------
wf_dat <- st_transform(wf_dat, crs = 3857)  # Use a CRS with meters as units
# Calculate the area of each geometry (in square meters)
wf_dat <- wf_dat %>%
mutate(wildfire_area = as.numeric(st_area(geometry)))
# assign buffer distances based on wildfire area
wf_dat <- wf_dat %>%
mutate(
buffer_dist_main = case_when(wildfire_area < 1000 ~ 5000, wildfire_area >= 1000 ~ 10000),
buffer_dist_smaller = case_when(wildfire_area < 1000 ~ 3000, wildfire_area >= 1000 ~ 5000),
buffer_dist_larger = case_when(wildfire_area < 1000 ~ 10000, wildfire_area >= 1000 ~ 20000)
)
months <- seq(as.Date("2000-01-01"), as.Date("2018-12-01"), by = "month")
# All ---------------------------------------------------------------------
all <- wf_dat %>%
select(ID_hazard = wildfire_id,
buffer_dist_main,
buffer_dist_smaller,
buffer_dist_larger,
month_exposed)
for (i in 1:length(months)) {
main_mon <- main %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
main_mon,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
# Divide wf disaster data into monthly files to create monthly exposure
# This will be based on ignition dates only
# Libraries ---------------------------------------------------------------
pacman::p_load(tidyverse, here, sfarrow, sf)
# Read --------------------------------------------------------------------
wf_dat <- st_read(here('local_data', 'wfbz_disasters.geojson'))
# Make monthly files ------------------------------------------------------
wf_dat <-
wf_dat %>%
mutate(month_exposed = lubridate::floor_date(wildfire_ignition_date, unit = 'month')) %>%
filter(month_exposed >= as.Date("2000-01-01") & month_exposed < as.Date("2019-01-01"))
# Assign buffers ----------------------------------------------------------
wf_dat <- st_transform(wf_dat, crs = 3857)  # Use a CRS with meters as units
# Calculate the area of each geometry (in square meters)
wf_dat <- wf_dat %>%
mutate(wildfire_area = as.numeric(st_area(geometry)))
# assign buffer distances based on wildfire area
wf_dat <- wf_dat %>%
mutate(
buffer_dist_main = case_when(wildfire_area < 1000 ~ 5000, wildfire_area >= 1000 ~ 10000),
buffer_dist_smaller = case_when(wildfire_area < 1000 ~ 3000, wildfire_area >= 1000 ~ 5000),
buffer_dist_larger = case_when(wildfire_area < 1000 ~ 10000, wildfire_area >= 1000 ~ 20000)
)
months <- seq(as.Date("2000-01-01"), as.Date("2018-12-01"), by = "month")
# All ---------------------------------------------------------------------
all <- wf_dat %>%
select(ID_hazard = wildfire_id,
buffer_dist_main,
buffer_dist_smaller,
buffer_dist_larger,
month_exposed)
for (i in 1:length(months)) {
main_mon <- main %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
main_mon,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
for (i in 1:length(months)) {
main_mon <- main %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
all,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
for (i in 1:length(months)) {
main_mon <- all %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
all,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
for (i in 1:length(months)) {
main_mon <- all %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
all,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
# Divide wf disaster data into monthly files to create monthly exposure
# This will be based on ignition dates only
# Libraries ---------------------------------------------------------------
pacman::p_load(tidyverse, here, sfarrow, sf)
# Read --------------------------------------------------------------------
wf_dat <- st_read(here('local_data', 'wfbz_disasters.geojson'))
# Make monthly files ------------------------------------------------------
wf_dat <-
wf_dat %>%
mutate(month_exposed = lubridate::floor_date(wildfire_ignition_date, unit = 'month')) %>%
filter(month_exposed >= as.Date("2000-01-01") & month_exposed < as.Date("2019-01-01"))
# Assign buffers ----------------------------------------------------------
wf_dat <- st_transform(wf_dat, crs = 3857)  # Use a CRS with meters as units
# Calculate the area of each geometry (in square meters)
wf_dat <- wf_dat %>%
mutate(wildfire_area = as.numeric(st_area(geometry)))
# assign buffer distances based on wildfire area
wf_dat <- wf_dat %>%
mutate(
buffer_dist_main = case_when(wildfire_area < 1000 ~ 5000, wildfire_area >= 1000 ~ 10000),
buffer_dist_smaller = case_when(wildfire_area < 1000 ~ 3000, wildfire_area >= 1000 ~ 5000),
buffer_dist_larger = case_when(wildfire_area < 1000 ~ 10000, wildfire_area >= 1000 ~ 20000)
)
months <- seq(as.Date("2000-01-01"), as.Date("2018-12-01"), by = "month")
# All ---------------------------------------------------------------------
all <- wf_dat %>%
select(ID_hazard = wildfire_id,
buffer_dist_main,
buffer_dist_smaller,
buffer_dist_larger,
month_exposed)
for (i in 1:length(months)) {
main_mon <- all %>%
filter(month_exposed == months[i]) %>%
select(-month_exposed)
st_write(
main_mon,
here(
"local_data",
"monthly_wf_exposure",
"all_analysis",
paste0("month_", months[i], ".geojson")
),
driver = "GeoJSON",
delete_dsn = TRUE
)
}
# Load the tigris package
library(here)
library(tigris)
library(sf)
library(tidyverse)
# Set options to cache the data locally
options(tigris_use_cache = TRUE)
# Download 2020 ZCTAs
zctas_2020 <- zctas(year = 2020)
# View the first few rows
head(zctas_2020)
zctas_2020 <- zctas_2020 %>% select(ID_admin_unit = ZCTA5CE20)
st_write(
zctas_2020,
here(
'local_data',
'zctas_2020.geojson'
),
driver = "GeoJSON",
delete_dsn = TRUE
)
zcta_list <- sort(unique(zctas_2020$ID_admin_unit))
zcta_list
write_rds(zcta_list, here("data_for_upload", 'zcta_list.RDS'))
pacman::p_load(tidyverse, here)
exp <- read_csv(here("local_data", 'binary_exposures_by_zcta.csv'))
exp <- read_csv(here("local_data", 'intermediate_data', 'binary_exposures_by_zcta.csv'))
glimpse(exp)
pacman::p_load(tidyverse, here)
exp <- read_csv(here(
"local_data",
'intermediate_data',
'binary_exposures_by_zcta.csv'
))
conus_temp <- read_rds(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.rds"
))
conus_temp <- read_rds(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.RDS"
))
load(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.RDS"
))
conus <- read_csv(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.RDS"
))
conus <- read_csv(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.csv"
))
problems(conus)
exp <- read.csv(here(
"local_data",
'intermediate_data',
'binary_exposures_by_zcta.csv'
))
ak_temp <- read.csv(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_ak.csv"
))
View(ak_temp)
conus <- read_csv(here(
"local_data",
"raw_data",
"era_5_temp_estimates",
"results_conus.csv"
))
conus_path = '/Volumes/squirrel-utopia/era_5/data/output/results_conus.csv'
conus <- read_csv(conus_path)
View(conus)
bind_rows(conus, hi)
pacman::p_load(tidyverse, here)
exp <- read_csv(here(
"local_data",
'intermediate_data',
'binary_exposures_by_zcta.csv'
))
conus_path = '/Volumes/squirrel-utopia/era_5/data/output/results_conus.csv'
conus <- read_csv(conus_path)
ak_path = '/Volumes/squirrel-utopia/era_5/data/output/results_ak.csv'
ak <- read_csv(ak_path)
hi_path = '/Volumes/squirrel-utopia/era_5/data/output/results_hi.csv'
hi <- read_csv(hi_path)
all <- bind_rows(conus, hi)
ak <- read_csv(ak_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
hi <- read_csv(hi_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
all <- bind_rows(conus, hi)
all <- bind_rows(all, ak)
glimpse(all)
all_long <- all %>% pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(date = sub("t2m_", "", date))
glimpse(all_long)
glimpse(exp)
all_long <- all %>% pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(date = sub("t2m_", "", date)) %>%
mutate(month = substr(date, start = 1, stop = 7))
glimpse(all_long)
glimpse(exp)
# Add temperature data to wildfire zcta exposure data
# Libraries ---------------------------------------------------------------
pacman::p_load(tidyverse, here)
# Read --------------------------------------------------------------------
exp <- read_csv(here(
"local_data",
'intermediate_data',
'binary_exposures_by_zcta.csv'
))
conus_path = '/Volumes/squirrel-utopia/era_5/data/output/results_conus.csv'
conus <- read_csv(conus_path)
ak_path = '/Volumes/squirrel-utopia/era_5/data/output/results_ak.csv'
ak <- read_csv(ak_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
hi_path = '/Volumes/squirrel-utopia/era_5/data/output/results_hi.csv'
hi <- read_csv(hi_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
all <- bind_rows(conus, hi)
all <- bind_rows(all, ak)
all_long <- all %>% pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(date = sub("t2m_", "", date)) %>%
mutate(month = substr(date, start = 1, stop = 7)) %>%
select(-date)
exp <- exp %>% left_join(all_long)
glimpse(exp)
write_csv(exp, here(
"local_data",
'intermediate_data',
'exposures_with_temp_for_upload.csv'
))
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/06_add_temp_data.R")
# Add temperature data to wildfire zcta exposure data
# Libraries ---------------------------------------------------------------
pacman::p_load(tidyverse, here)
# Read --------------------------------------------------------------------
exp <- read_csv(here(
"local_data",
'intermediate_data',
'binary_exposures_by_zcta.csv'
))
conus_temp_path = '/Volumes/squirrel-utopia/era_5/data/output/results_conus.csv'
conus_temp <- read_csv(conus_temp_path)
ak_temp_path = '/Volumes/squirrel-utopia/era_5/data/output/results_ak.csv'
ak_temp <- read_csv(ak_temp_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
hi_temp_path = '/Volumes/squirrel-utopia/era_5/data/output/results_hi.csv'
hi_temp <- read_csv(hi_temp_path) %>%
mutate(ID_admin_unit = as.character(ID_admin_unit))
all_temp <- bind_rows(list(conus_temp, ak_temp, hi_temp))
all_temp <- all_temp %>%
pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(month = substr(sub("t2m_", "", date)), start = 1, stop = 7)
all_temp <- all_temp %>%
pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(month = substr(sub("t2m_", "", date), start = 1, stop = 7))
glimpse(all_temp)
all_temp <- all_temp %>%
pivot_longer(
cols = starts_with("t2m_"),
names_to = "date",
values_to = "temperature"
) %>%
mutate(month = substr(sub("t2m_", "", date), start = 1, stop = 7)) %>%
select(-date)
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/06_add_temp_data.R")
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/04_find_percent_pop_exposed_by_zcta.R")
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/05_binary_exposure_assignment.R")
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/05_binary_exposure_assignment.R")
source("/Volumes/squirrel-utopia/national_wf_disaster_hosp/code/06_add_temp_data.R")
